Python uses etree from lxml library when serializing with the provxml module, when wasGeneratedBy is being serialized def serialize_bundle(self, bundle, element=None, force_types=False): is used.
If identifier is null, there is no xml-attribute being created :
if identifier:
    attrs = {_ns_prov("id"): identifier}
else:
    attrs = None
elem = etree.SubElement(xml_bundle_root, _ns_prov(rec_label), attrs).

Java, using jackson for xml deserialization has no issue deserializing xml element without prov:id xml-attribute.